{
  "name": "postvel",
  "version": "3.0.0",
  "description": "",
  "main": "index.js",
  "size-limit": [
    {
      "path": "build/index.html",
      "limit": "11 KB"
    }
  ],
  "scripts": {
    "start": "parcel src/index.pug --open",
    "build": "parcel build src/index.pug -d build",
    "deploy": "npm run build && cd build && now --prod",
    "size": "npm run build && size-limit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@size-limit/preset-app": "^2.2.2",
    "cssnano": "^4.1.10",
    "parcel-plugin-static-files-copy": "^2.2.1",
    "postcss-mixins": "^6.2.3",
    "precss": "^4.0.0",
    "pug": "^2.0.4"
  },
  "dependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/dtroode/postvel"
  }
}
